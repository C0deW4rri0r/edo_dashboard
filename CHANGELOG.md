# Журнал изменений

## Версия перед защитой

- Добавлено автоматическое создание папки `logs` при загрузке настроек Django.
- Добавлен `logs/.gitkeep`, чтобы структура проекта сохранялась в архиве.
- Файл `dockerfile` переименован в `Dockerfile`.
- Исправлены переменные окружения в `docker-compose.yml`.
- Добавлены smoke-тесты API в `dashboard/tests.py`.
- Исправлен расчет последних 12 месяцев в `/api/dynamics/`.
- README переписан с честным указанием демонстрационного статуса проекта.
- `requirements.txt` сохранен в UTF-8.
- Добавлен `.env.example` с минимальными настройками окружения.
